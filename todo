1. Hex grid and tile placement:
    ✓ Set up project @done(26-07-08 12:46)
    ✓ Very first version of creating a hex grid @done(26-07-08 12:46)

2. Hidden simulation values (Moisture, Temperature, Fertility, Elevation):
    ✔ When tiles are created, give them a set of starter values. @done(26-07-11 10:29)

3. Terrain evolution rules:
    ☐ Service to run through the tiles and apply the influences to its neighbors. Each tile is influenced equally.

4. Element harvesting:

5. Card system:
     ☐ Click the card
     ☐ select the tile to apply it to
     ☐ add the values to the cell values of the tile.
     ☐ If the change causes the tile to become a different type, this should happen now.

6. Objectives:

7. Expansion cards:

8. Visual polish and additional content:

---
UI:
    ✔ Add the root UI element. For now, it will be a 3D UI panel floating in front of the table. @done(26-07-11 10:32)
    ☐ Add a button to end the turn, so an update of the world can be triggered.
    ✔ Add a few POC cards to the UI @done(26-07-11 10:32)
    ☐ Make the card work. See 5.

General:
    ☐ Refactor tile data away from render component and let that only deal with the rendering
    ✔ Unhover @done(26-07-13 14:33)
    ✔ Somehow make the hover on tiles work AND make the menu work... Maybe 2 cursor components? Each with a different mask? @done(26-07-13 14:51)
    
System to show hovering over a tile:
    ☐ Component that handles interaction with a tile, should be on the tile
    ☐ Component that handles placing an indicator when hovering over a tile, is in root.
    Tile interaction service:
         that is called from these components that manages interaction
        ☐ methods for hovering, unhovering and clicking a tile
        ☐ events that are fired when hovering, unhovering and clicking a tile
            Events contain needed information, tile and position of the tile in world space.
        
---

# Short Thoughts

- When a tile is created, it has a set of starter values. Based on these the Manhatten distance is calculated to the nearest tile type. If the distance is less then a threshold, the tile becomes that type. If it is a specific type, it will start to influence its neighbors and might generate resources. By playing cards, the player can nudge tile values and cause them to evolve.

- Maybe the UI should move to the side of the table you're closest. So that if you move around the table, it's always somewhat in front of you.